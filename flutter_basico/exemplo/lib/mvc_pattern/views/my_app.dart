import 'package:flutter/material.dart';

import 'estrutura_inicial.dart';

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: EstruturaInicial(),
    );
  }
}
